#pragma once
#include <string>

class Account
{
protected:
    std::string login;
    std::string password;
    bool isWorker;
    std::string firstName;
    std::string lastName;
    std::string email;
    std::string phoneNumber;

public:
    
};